# Copy to .env and adjust if needed.
# NeuroBridge loads these via config.py (python-dotenv).

# Optional: path to Edge for Read Aloud (voice output).
# EDGE_PATH=C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe
